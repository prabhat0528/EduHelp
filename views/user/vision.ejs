```html
<% layout("/layouts/boilerplate") %>

<div 
  class="relative min-h-screen bg-cover bg-center bg-no-repeat" 
  style="background-image: url('https://images.unsplash.com/photo-1524178232363-1fb2b075b655?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80');"
  aria-label="Background image of a classroom for Our Vision page"
>
  <!-- Overlay with gradient -->
  <div class="absolute inset-0 bg-gradient-to-b from-black/70 to-black/50"></div>

  <!-- Content -->
  <section 
    class="relative z-10 flex flex-col justify-center min-h-screen w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-12 animate-fade-in" 
    aria-labelledby="vision-heading"
  >
    <h2 
      id="vision-heading" 
      class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-8 sm:mb-12 relative"
    >
      Our Vision
      <span class="absolute -bottom-2 left-0 w-1/3 h-1 bg-gradient-to-r from-primary to-secondary"></span>
    </h2>

    <article class="space-y-6 sm:space-y-8">
      <div 
        class="point bg-white/10 border-l-4 border-primary p-4 sm:p-6 rounded-lg shadow-md animate-point-in" 
        style="animation-delay: 0.2s;"
      >
        <h3 class="text-xl sm:text-2xl lg:text-3xl font-semibold text-white [text-shadow:_0_2px_4px_rgba(0,0,0,0.5)]">
          1. Empowering Lifelong Learning
        </h3>
        <p class="text-sm sm:text-base lg:text-lg text-gray-200 mt-2">
          We envision a world where education is lifelong, continuous, and accessible to all. Our goal is to provide personalized learning experiences that allow learners to expand their knowledge and skillset at any stage in their life, regardless of their location.
        </p>
      </div>

      <div 
        class="point bg-white/10 border-l-4 border-primary p-4 sm:p-6 rounded-lg shadow-md animate-point-in" 
        style="animation-delay: 0.4s;"
      >
        <h3 class="text-xl sm:text-2xl lg:text-3xl font-semibold text-white [text-shadow:_0_2px_4px_rgba(0,0,0,0.5)]">
          2. Democratizing Education
        </h3>
        <p class="text-sm sm:text-base lg:text-lg text-gray-200 mt-2">
          Our mission is to break down barriers to education. By leveraging cutting-edge technology, we aim to provide equal access to world-class educational resources, enabling individuals to learn, grow, and succeed on their own terms.
        </p>
      </div>

      <div 
        class="point bg-white/10 border-l-4 border-primary p-4 sm:p-6 rounded-lg shadow-md animate-point-in" 
        style="animation-delay: 0.6s;"
      >
        <h3 class="text-xl sm:text-2xl lg:text-3xl font-semibold text-white [text-shadow:_0_2px_4px_rgba(0,0,0,0.5)]">
          3. Building a Global Learning Community
        </h3>
        <p class="text-sm sm:text-base lg:text-lg text-gray-200 mt-2">
          Education should be a collaborative and inclusive experience. We strive to create a global network of learners and educators, where diverse ideas are shared, innovation is encouraged, and everyone has the opportunity to contribute to collective progress.
        </p>
      </div>
    </article>
  </section>
</div>

<script>
  // Ensure animations are applied after DOM load
  document.addEventListener('DOMContentLoaded', () => {
    const content = document.querySelector('.animate-fade-in');
    if (content) {
      content.style.animation = 'fadeIn 2s ease-in-out forwards';
    }
  });
</script>

<style>
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes pointIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .animate-fade-in {
    opacity: 0;
    animation: fadeIn 2s ease-in-out forwards;
  }

  .animate-point-in {
    opacity: 0;
    animation: pointIn 0.8s ease-out forwards;
  }
</style>
```